# 🚀 دليل GitHub Actions - الأسهل والأفضل!

## لماذا GitHub Actions؟

✅ **مجاني 100%** (2000 دقيقة/شهر)  
✅ **سهل جداً** - لا يحتاج VPS  
✅ **يعمل تلقائياً** كل 5 دقائق  
✅ **البيانات محفوظة** في الـ repo  
✅ **تحميل سهل** - CSV جاهز للتنزيل  

---

## 📋 خطوات التشغيل

### 1. إنشاء Repository على GitHub

1. افتح [github.com](https://github.com)
2. سجّل دخول
3. اضغط **"+"** → **"New repository"**
4. الاسم: `crash-monitor`
5. اختر **Public** أو **Private**
6. ✅ فعّل **"Add a README file"**
7. اضغط **"Create repository"**

---

### 2. رفع الملفات

#### الطريقة 1: عبر واجهة GitHub (الأسهل)

1. في صفحة الـ repo، اضغط **"uploading an existing file"**
2. اسحب **جميع** الملفات والمجلدات من مجلد `github`:
   - `crash_monitor_github.py`
   - المجلد `.github` (كامل)
   - `.gitignore`
   - `README.md`
   - `GITHUB_SETUP.md`
   - `START_HERE.txt`
3. اضغط **"Commit changes"**

#### الطريقة 2: عبر Git (للمحترفين)

```bash
cd "c:\Users\ELOott\Desktop\New folder (2)\github"
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/crash-monitor.git
git branch -M main
git push -u origin main
```

---

### 3. تفعيل GitHub Actions

1. في الـ repo، اذهب لتبويب **"Actions"**
2. ستجد رسالة: **"Workflows aren't being run on this forked repository"**
3. اضغط **"I understand my workflows, go ahead and enable them"**
4. اضغط على الـ workflow **"Crash Monitor 24/7"**
5. اضغط زر **"Enable workflow"** (إذا ظهر)

---

### 4. التشغيل اليدوي (أول مرة)

1. في تبويب **Actions**
2. على اليسار، اختر **"Crash Monitor 24/7"**
3. على اليمين، اضغط **"Run workflow"**
4. اضغط الزر الأخضر **"Run workflow"**
5. ✅ سيبدأ فوراً!

انتظر دقيقة ثم حدّث الصفحة - ستجد workflow run جديد يعمل.

---

## 🔄 كيف يعمل؟

### التشغيل التلقائي

- **كل 5 دقائق**: GitHub يشغل الـ workflow تلقائياً
- **المدة**: كل تشغيلة تراقب لمدة **6 دقائق**
- **الحفظ**: يحفظ البيانات في:
  - `crash_records.json`
  - `crash_records.csv`
- **الرفع**: يرفع البيانات للـ repo تلقائياً

### المراقبة

1. يفتح المتصفح headless
2. يدخل على `faucetpay.io/crash`
3. يفحص كل **3 ثواني**
4. يحفظ القيم الجديدة
5. يرفع للـ repo تلقائياً

---

## 📥 تحميل البيانات

### الطريقة 1: من الـ Repo مباشرة

1. افتح ملف `crash_records.csv` في الـ repo
2. اضغط زر **"Download"** أو **"Raw"**
3. احفظ الملف
4. افتحه في Excel ✅

### الطريقة 2: من Artifacts

1. في تبويب **Actions**
2. اضغط على أي workflow run
3. أسفل الصفحة → **Artifacts**
4. حمّل **"crash-records.zip"**
5. فك الضغط → افتح CSV

---

## ⚙️ تخصيص المراقبة

### تغيير وقت التشغيل التلقائي

عدّل ملف `.github/workflows/monitor.yml`:

```yaml
on:
  schedule:
    - cron: '*/5 * * * *'   # كل 5 دقائق (الافتراضي)
    # أو
    - cron: '*/10 * * * *'  # كل 10 دقائق
    # أو
    - cron: '0 * * * *'     # كل ساعة
    # أو
    - cron: '0 */2 * * *'   # كل ساعتين
```

**ملاحظة:** Cron format:
```
* * * * *
│ │ │ │ │
│ │ │ │ └─ يوم الأسبوع (0-6)
│ │ │ └─── الشهر (1-12)
│ │ └───── اليوم (1-31)
│ └─────── الساعة (0-23)
└───────── الدقيقة (0-59)
```

### تغيير مدة كل تشغيلة

في `.github/workflows/monitor.yml`، عدّل:

```yaml
- name: Run monitor
  env:
    MONITOR_DURATION: '10'  # 10 دقائق بدلاً من 6
  run: |
    python crash_monitor_github.py
```

---

## 🎯 المميزات

### 1. التشغيل المستمر 24/7
- يعمل كل 5 دقائق تلقائياً
- لا يحتاج أي تدخل منك
- يستمر حتى لو أغلقت الكمبيوتر

### 2. البيانات آمنة
- محفوظة في الـ repo
- Backup تلقائي في Artifacts (90 يوم)
- تاريخ كامل في Git commits

### 3. سهولة الوصول
- افتح GitHub → شاهد السجلات
- حمّل CSV مباشرة
- JSON API جاهز

---

## 📊 مثال على السجلات

### crash_records.csv
```csv
ID,Crash Value,Timestamp
1708350000000,1.52x,2024-02-19 12:30:45
1708349900000,2.34x,2024-02-19 12:28:20
1708349800000,1.98x,2024-02-19 12:26:40
```

### crash_records.json
```json
[
  {
    "id": 1708350000000,
    "crash_value": "1.52x",
    "timestamp": "2024-02-19 12:30:45"
  },
  {
    "id": 1708349900000,
    "crash_value": "2.34x",
    "timestamp": "2024-02-19 12:28:20"
  }
]
```

---

## ❓ الأسئلة الشائعة

**س: كم دقيقة مجانية في الشهر؟**  
ج: **2000 دقيقة مجاناً**، كافية لـ **330+ ساعة مراقبة** (كل شهر يُعاد)

**س: هل يعمل بعد إغلاق الكمبيوتر؟**  
ج: **نعم!** يعمل على خوادم GitHub

**س: كيف أوقف المراقبة؟**  
ج: Actions → اختر الـ workflow → **Disable workflow**

**س: كيف أحذف السجلات القديمة؟**  
ج: احذف/عدّل ملفات `crash_records.json` و `crash_records.csv` في الـ repo

**س: هل يمكن استخدامه مع repo خاص؟**  
ج: **نعم!** الـ Actions تعمل مع Private repos

**س: ماذا لو نفذت الدقائق المجانية؟**  
ج: 
- **Public repos**: دقائق غير محدودة! ✅
- **Private repos**: 2000 دقيقة/شهر
- يمكنك تقليل تكرار التشغيل (مثلاً كل 15 دقيقة)

**س: هل البيانات آمنة؟**  
ج: نعم، محفوظة في الـ repo + Artifacts. يمكنك جعل الـ repo خاص.

---

## 🔧 حل المشاكل

### المشكلة: Workflow لا يعمل تلقائياً

**الحل:**
1. تأكد أن الـ repo **Public** أو لديك GitHub Pro/Team
2. في **Settings** → **Actions** → **General**
3. تحقق أن **"Allow all actions"** مفعّل
4. تحقق أن **Scheduled workflows** مفعّل

### المشكلة: خطأ في التثبيت

**الحل:**
- تحقق من ملف `.github/workflows/monitor.yml`
- تأكد أنه في المسار الصحيح: `.github/workflows/`
- تأكد من صحة YAML syntax

### المشكلة: لا توجد بيانات محفوظة

**الحل:**
1. افتح آخر workflow run في Actions
2. اقرأ الـ logs
3. ابحث عن أخطاء
4. تحقق من أن الموقع يعمل

### المشكلة: الـ workflow يتوقف بعد 6 ساعات

**الحل:**
- هذا طبيعي (حد GitHub)
- لكن سيعمل تلقائياً في الجولة التالية (بعد 5 دقائق)

---

## 🎉 نصائح إضافية

### 1. مشاهدة السجلات المباشرة

في تبويب Actions → اضغط على workflow run نشط → شاهد الـ logs مباشرة

### 2. إشعارات Telegram (اختياري)

أضف هذا في نهاية الـ workflow:

```yaml
- name: Send Telegram notification
  if: success()
  env:
    TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
    TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
  run: |
    RECORD_COUNT=$(wc -l < crash_records.csv)
    curl -s -X POST https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage \
      -d chat_id=${TELEGRAM_CHAT_ID} \
      -d text="✅ تم تسجيل ${RECORD_COUNT} سجل"
```

ثم في **Settings** → **Secrets** → أضف:
- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_CHAT_ID`

### 3. Backup يومي

البيانات محفوظة تلقائياً في:
- الـ repo (Git history كامل)
- Artifacts (90 يوم)

### 4. استخدام API

البيانات JSON متاحة عبر:
```
https://raw.githubusercontent.com/YOUR_USERNAME/crash-monitor/main/crash_records.json
```

---

## 📞 الدعم

إذا واجهت مشاكل:
1. تحقق من تبويب **Actions** → Logs
2. ابحث عن رسائل الخطأ باللون الأحمر
3. تأكد من صلاحيات الـ repo
4. افتح [Issue](../../issues) في الـ repo

---

## 🏆 الخلاصة

**GitHub Actions** هو **أفضل حل مجاني** للمراقبة 24/7:

✅ لا يحتاج VPS  
✅ لا يحتاج Replit  
✅ لا يحتاج كمبيوترك مفتوح  
✅ سهل جداً  
✅ مجاني للأبد (للـ public repos)  
✅ موثوق وسريع  

---

## 📚 موارد إضافية

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Cron Expression Generator](https://crontab.guru/)
- [Playwright Documentation](https://playwright.dev/python/)

---

**بالتوفيق!** 🎰
