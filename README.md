# 🎰 Crash Monitor

مراقبة وتسجيل أرقام الكراش تلقائياً 24/7

## 🚀 البدء السريع

### 1. Fork أو Clone هذا الـ Repo

```bash
git clone https://github.com/YOUR_USERNAME/crash-monitor.git
cd crash-monitor
```

### 2. رفع للـ GitHub

إذا بدأت من الصفر:

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/crash-monitor.git
git push -u origin main
```

### 3. تفعيل GitHub Actions

1. اذهب لتبويب **Actions** في الـ repo
2. اضغط **"I understand my workflows, go ahead and enable them"**
3. اضغط على workflow **"Crash Monitor 24/7"**
4. اضغط **"Run workflow"** → **"Run workflow"**
5. ✅ سيعمل تلقائياً كل 5 دقائق!

---

## 📊 تحميل البيانات

### الطريقة 1: من الـ Repo مباشرة

- افتح ملف `crash_records.csv` في الـ repo
- اضغط زر **"Download"** أو **"Raw"**
- افتحه في Excel ✅

### الطريقة 2: من Artifacts

1. في تبويب **Actions**
2. اضغط على آخر workflow run
3. أسفل الصفحة → **Artifacts**
4. حمّل **"crash-records"**

---

## 🔄 كيف يعمل؟

- **تلقائياً**: يعمل كل **5 دقائق**
- **المدة**: كل تشغيلة تراقب لمدة **6 دقائق**
- **الحفظ**: يحفظ في:
  - `crash_records.json` (للبرمجة)
  - `crash_records.csv` (لـ Excel)

---

## 📊 مثال على السجلات

```csv
ID,Crash Value,Timestamp
1708350000000,1.52x,2024-02-19 12:30:45
1708349900000,2.34x,2024-02-19 12:28:20
1708349800000,1.98x,2024-02-19 12:26:40
```

---

## ⚙️ التخصيص

### تغيير وقت التشغيل

عدّل `.github/workflows/monitor.yml`:

```yaml
on:
  schedule:
    - cron: '*/5 * * * *'   # كل 5 دقائق
    # - cron: '*/10 * * * *'  # كل 10 دقائق
    # - cron: '0 * * * *'     # كل ساعة
```

### تغيير مدة المراقبة

عدّل في `.github/workflows/monitor.yml`:

```yaml
- name: Run monitor
  env:
    MONITOR_DURATION: '10'  # 10 دقائق
```

---

## 🏆 المميزات

✅ **مجاني 100%** (2000 دقيقة/شهر)  
✅ **يعمل 24/7** على خوادم GitHub  
✅ **لا يحتاج VPS**  
✅ **لا يحتاج كمبيوترك مفتوح**  
✅ **حفظ تلقائي** JSON + CSV  
✅ **Backup تلقائي** في Artifacts  

---

## 📁 هيكل المشروع

```
crash-monitor/
├── .github/
│   └── workflows/
│       └── monitor.yml          # GitHub Actions workflow
├── crash_monitor_github.py      # السكريبت الرئيسي
├── .gitignore
├── README.md
├── GITHUB_SETUP.md              # دليل مفصّل
├── START_HERE.txt               # دليل سريع
├── crash_records.json           # البيانات (يُنشأ تلقائياً)
└── crash_records.csv            # البيانات CSV (يُنشأ تلقائياً)
```

---

## ❓ الأسئلة الشائعة

**س: كم دقيقة مجانية؟**  
ج: 2000 دقيقة/شهر (كافية لـ 330+ ساعة)

**س: هل يعمل بعد إغلاق الكمبيوتر؟**  
ج: نعم! يعمل على خوادم GitHub

**س: كيف أوقف المراقبة؟**  
ج: Actions → اختر الـ workflow → Disable workflow

**س: هل يمكن استخدام repo خاص؟**  
ج: نعم! Actions تعمل مع Private repos

---

## 📞 الدعم

للمشاكل أو الأسئلة:
- افتح [Issue](../../issues)
- اقرأ [GITHUB_SETUP.md](GITHUB_SETUP.md)

---

## 📄 الترخيص

MIT License - استخدمه بحرية!

---

**⭐ إذا أعجبك المشروع، اضغط Star!**
